import { FormHoverBorderDirective } from './form-hover-border.directive';

describe('FormHoverBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new FormHoverBorderDirective();
    expect(directive).toBeTruthy();
  });
});
